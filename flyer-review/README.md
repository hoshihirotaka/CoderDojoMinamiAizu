# チラシレビュー運用ルール

目的: Canvaで作るチラシの内容を安全にレビューするための一時フォルダ。

## 使い方
- このフォルダにPDFを入れる（例: `flyer.pdf`）。
- 私（Codex）が内容を確認して提案を作成する。
- 提案は `proposals.md` に追記する。
- 提案が完了したら、PDFと提案ファイルは削除する（個人情報を残さないため）。

## 運用ポリシー
- GitHubにはpushしない。
- 個人情報が含まれる可能性があるため、内容はこのフォルダ内だけで扱う。
- 作業が完了したら都度削除する。
