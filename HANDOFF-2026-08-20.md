# 引き継ぎ 2026-08-20（LPの計測の読み込みを直す）

ブロマス（PlayMakerLabLP）側で計測を整備した際に、このLPにも同じ問題が
残っていることが分かったため、別セッションで対応する。

---

## 次のセッションに貼るプロンプト

```
CoderDojo南会津のLPの計測を直す作業です。

前提として以下を読んでください。
- CoderDojoMinamiAizu の HANDOFF-2026-08-20.md（このタスクの内容）
- CoderDojoMinamiAizu の AGENTS.md
- /Users/hirotakahoshi/PlayMakerLabLP/index.html の155行目付近（移植元のコードとコメント）

作業リポジトリは /Users/hirotakahoshi/CoderDojoMinamiAizu です。
mainブランチにいるので、作業前にブランチを切ってください。
```

---

## やること

**GA4の読み込みを、遅延ロードから通常のasync読み込みに戻す。**

`index.html` の末尾（504行目付近）が、いまこうなっている。

```js
window.addEventListener("load", () => {
  if ("requestIdleCallback" in window) {
    window.requestIdleCallback(loadAnalytics, { timeout: 8000 });
    return;
  }
  window.setTimeout(loadAnalytics, 8000);
}, { once: true });
```

**`window.load`（全画像の読み込み完了）を待ったうえで、さらに最大8秒待つ。**

`pointerdown` / `scroll` / `keydown` / `touchstart` でも読み込むので全滅はしないが、
**着地して何も触らずに数秒で帰った人は `page_view` すら記録されない。**

### なぜ直すか（ブロマス側で実証済み）

PlayMakerLabLP は以前これと同じ設計で、やめている。`index.html` にコメントが残っている。

> 以前はGA4だけ window.load 後に読み込んでいたが、全画像の読み込み完了を待つため
> モバイルで数秒遅れ、早期離脱がまるごと計測から漏れていた。エンゲージメント時間も
> 実態より短く出る。計測の起点を早めるため、通常のasync読み込みに戻している。

**クリックイベントの穴より、こちらのほうがずっと大きい。** 「0件」が「起きなかった」なのか
「測っていなかった」なのか区別できなくなる。

### 移植元

`/Users/hirotakahoshi/PlayMakerLabLP/index.html` の155行目付近。
測定IDだけ `G-DSETWZS3SZ` に置き換える（**Google広告のタグ `AW-...` は入れない**。
CoderDojoはGoogle広告を使わない方針）。

**→ 2026-08-20に実施済み。** `<head>` の末尾（stylesheetの後）に配置。
ヒーロー画像の `preload` より後ろに置いて、LCPの優先度を落とさないようにしている。

---

## ⚠️ 直した日が「境界日」になる

**修正後、セッション数と `page_view` が増える。これは成果ではない。**
これまで計測から漏れていた人が記録されるようになるだけ。

- **修正日をまたいだ前後比較をしない**
- 増加を「アクセスが増えた」と読まない
- 修正日を記録しておくこと（このファイルに追記する）

### 境界日：2026-08-20（実施済み）

この日にGA4を即時読み込みへ変更した。**`page_view`／セッション数はこの日を境に増える。
8/20をまたいだ前後比較はしない。**

同じことがブロマス側でも起きていて、`~/curriculum/運営/LPアナリティクス/README.md` の
「指標ごとの有効な起点」に整理してある。考え方はそちらを参照。

---

## 広告を出している（重要）

**このLPには広告から流入がある。** これが前提を変える。

### ① 遅延ロードは「コンバージョンの取りこぼし」になりうる

広告をクリックして着地し、**スクロールもせずに申込みボタンを押した人**を考える。

`pointerdown` で読み込みは始まるが、`gtag.js` のダウンロードと初期化が終わる前に
Doorkeeperへ遷移すると、**キューに積まれた `apply_click` は送信されないまま消える。**

つまり**いちばん優良な訪問者（即決した人）から順に落ちる。** 広告の入札に
`apply_click` を使っているなら、**信号が薄くなって配信が絞られる**。
ブロマス側で2026-08-18に `doorkeeper_click` が止まって配信が落ちたのと同じ構図。

**最優先で直す理由がこれ。**

### ② 最初に確認すること → 2026-08-20に確認済み

| 確認 | 回答（2026-08-20） |
|---|---|
| どの媒体か | **Metaのみ（これから出稿予定）。Google広告は今後も使わない**（`AW-` タグは不要） |
| `apply_click` をコンバージョンとして使っているか | **一旦白紙。** ブロマスとは事情が違うため、コンバージョン設計はやり直す |
| Metaピクセルは入っているか | **入っていない**（`fbq` は0件）。出稿前に導入が必要 |
| 広告の着地先 | **未定。** 次回開催日が決まったので、そのあとに検討 |

### 未処理（次にやること）

> **2026-08-24：次回開催（第7回・9/27）のリンクが出たので1〜3を実施済み。**
> 残りは4のみ。

1. ~~次回開催のLP掲載~~ → **完了**（第7回 2026-09-27 / `events/199000`）
2. ~~`apply_click` の復活~~ → **完了**（申込みボタンに `js-apply-button` と `id="apply-button"` を付与）
3. ~~申込み以外のボタンのクリック計測~~ → **完了**（`button_click` / `[data-track]`）
4. ~~Doorkeeper「申込完了」のキーイベント化~~ → **完了**（`signup_complete` / 2026-08-24に発火確認）

**申し送りの項目はすべて消化済み。** 残るのはGA4管理画面での作業のみ。

#### カスタムディメンションの登録（**LP側のプロパティ** `G-DSETWZS3SZ`）

```
GA4 → 管理 → データの表示 → カスタム定義 → 「カスタム ディメンションを作成」
```

| ディメンション名 | 範囲 | イベント パラメータ |
|---|---|---|
| ナビの遷移先 | **イベント** | `section` |
| ボタン | **イベント** | `button` |

- **範囲は必ず「イベント」**（「ユーザー」にすると別物になる）
- **遡及しない。** 登録した時点より前のデータはレポートに出ないままになるため、
  登録が遅れた日数分がそのまま失われる
- 反映に24〜48時間。登録前でもDebugView・リアルタイムでは値が見える
- Doorkeeper側は**登録不要**。`signup_complete` の `page_location` は標準ディメンション

### 見送ったもの（2026-08-20の判断）

| | 理由 |
|---|---|
| **Metaピクセル** | **ブースト運用なら不要。** 申込が完了するのはDoorkeeperで、そこにピクセルは置けない＝返す先が無い。クリック数と単価はMeta管理画面で取れる |
| **クロスドメイン測定** | 広告はDoorkeeper直行でLPを経由していない（`lp / referral` は3ヶ月で10セッション）。繋いでも配信は良くならず、Doorkeeper側の既存データを捨てることになる |
| **Google広告関連（`AW-`）** | 今後も使わない方針 |

**※ プロパティを分けたままにしたので、LP→Doorkeeperリンクの
`utm_source=lp&utm_medium=referral` は外さないこと。** 別プロパティである以上、
Doorkeeperに来た人がLP経由かどうかを知る唯一の手段になっている。

### Doorkeeper「申込完了」のキーイベント化（手順）

**作業場所を間違えないこと。** これは**Doorkeeper側のプロパティ**（`ig / paid` や
`ticket / email` が流入元に出るほう）での作業。**LP側（`G-DSETWZS3SZ`）ではない。**

Doorkeeperのコードは触れない。**GA4の管理画面だけで完結する。**

#### 完了ページのURL（2026-08-24 特定済み）

ランディングページのレポートに実在が確認できた。**申し込み直す必要はない。**

```
/events/{イベントID}/tickets/{トークン}/completed
例: /events/196200/tickets/-yS6yCU2-5BvbxvSzeMe/completed
```

トークンは申込ごとに変わるため、**固定文字列では一致させられない。**
また**キャンセル側にも `/cancel` のパスが存在する**ので、単純に `/completed` を含む
条件にすると、将来キャンセル完了ページ（`/cancel/completed` 等）を誤って拾う恐れがある。
**正規表現で指定する。**

#### STEP 1: イベントを作成する

```
GA4 → 管理 → データの表示 → イベント → 「イベントを作成」→ 作成

  カスタムイベント名: signup_complete
  一致条件:
    event_name      次と等しい          page_view
    page_location   次の正規表現に一致   /tickets/[^/]+/completed
```

**「ソースイベントからパラメータをコピー」に必ずチェックを入れる**（初期状態は外れている）。
入れないと `signup_complete` に `page_location` が乗らず、**どの回の申込か区別できなくなる**。

> **なぜ「次で終わる `/completed`」ではないか**
> URLの末尾にクエリ（`?utm_...` など）が付くと一致しなくなるため。
> 正規表現なら途中一致なのでクエリが付いても拾える。

#### STEP 2: キーイベントに指定する

```
GA4 → 管理 → データの表示 → キーイベント → 「新しいキーイベント」
  → イベント名に signup_complete と入力
```

**まだ一度も発生していないイベント名でも、手入力すれば登録できる。**

#### 注意

- **さかのぼらない。** 作成後に発生したデータにしか適用されない（過去分は集計されない）
- 反映に**24〜48時間**かかることがある。すぐ0件でも壊れてはいない
- 元の `page_view` は消えない。**`signup_complete` が追加で発生する**形になるので、
  イベント総数が増えるのは正常
- **GA4のキーイベントはMetaの最適化には効かない。** 自分たちが判断するための指標

#### ⚠️ ランディングページのレポートで申込数を数えないこと

`/completed` は**申込フローの途中で到達する**ページで、セッションの入口ではない。
ランディングページのレポートは**セッションの最初のページしか出さない**ため、
そこに出る `/completed` の件数は**実際の申込数ではない**（大幅に少なく出る）。

- 申込の実数を見る → **Doorkeeperの管理画面**（これが正）
- ページビューとして見る → **レポート → エンゲージメント → ページとスクリーン**
- 流入元別に見る → キーイベント化後、**トラフィック獲得**のキーイベント列

### ✅ 検証記録（2026-08-24）

**LP側・Doorkeeper側とも、リアルタイムで発火を確認済み。**

| プロパティ | 確認できたイベント |
|---|---|
| LP（`G-DSETWZS3SZ`） | `apply_click` / `button_click` / `click`（拡張計測の離脱リンク） / `page_view` / `first_visit` / `session_start` |
| Doorkeeper（`G-BWCXQ6HMN7`） | `page_view` / **`signup_complete`** |

- **`apply_click` の復活を確認。** 2026-08-06〜08-24の間は発火していなかった
- **`button_click` の新規追加も動作確認済み**
- **`signup_complete` の発火を確認。** 正規表現 `/tickets/[^/]+/completed` は正しく一致している
- `nav_click` はこの検証中にナビを押していないため未確認（実装は同じ形）

#### 「データを受信していません」は正常だった

検証前、Doorkeeperのストリーム詳細に「過去48時間に受信したデータはありません」と
出ていたが、**設定の不具合ではなかった。**

このプロパティの流量は **1/1〜8/24の236日で618セッション＝1日あたり約2.6件**。
しかも平均ではなく、**告知のたびに跳ねる山型**。確認した時点は
「8/16が中止・9/27は未告知・広告も停止中」という谷の底だったため、
48時間ゼロでも異常ではない。

> **ストリームが1つしかないプロパティで過去データがあるなら、そのデータはその
> ストリームを通っている。** 「受信していません」の表示だけで設定を疑わないこと。

#### ⚠️ `signup_complete` はページビュー基準

完了ページの**再読み込みや再訪でも加算される**ため、**申込の実数とは一致しない**
（検証時も2件計上された）。

- **申込の実数 → Doorkeeperの管理画面が正**
- GA4で見るべきは**流入元別の内訳**（どの経路が申込に繋がったか）

#### ⚠️ 2026-08-24は内部トラフィックが混ざっている

検証のため本人のアクセスと申込操作が入っている。ブロマス側の8/18・8/20と同じ扱いで、
**この日を比較の起点にしないこと。**

### ⚠️ `apply_click` が2026-08-06〜08-24に発火していなかった（解消済み）

**同じ事故を繰り返さないために残す。**

中止対応のコミット `e5edd42`（2026-08-06）で、開催カードの申込みボタンを
「Doorkeeperで詳細を確認」リンクへ差し替えた際、**`.js-apply-button` ごと外した。**
中止イベントで誤カウントさせない意図だったが、結果として**計測クラスを持つ要素が
HTMLから消え**、`querySelectorAll` が何も拾わずリスナーが1つも付かない状態になった。
ハンドラは残っていたので、コードを見ても壊れているように見えないのが厄介だった。

**2026-08-24に第7回（9/27）を掲載した際、`js-apply-button` と `id="apply-button"` を
付け直して解消。**

> **教訓：開催情報を差し替えるときは、申込みボタンの計測クラスを必ずセットで確認する。**
> レポート上、「申込0件」と「計測されていない」は区別が付かない。
> `AGENTS.md` の「LPでの計測」にも注意書きを入れてある。

### ③ クリック計測の追加は「①のあと」→ 2026-08-20に `nav_click` を追加

`nav_click` などの追加は、ブロマス側と同じ理屈で価値がある（どこに関心があるか、
エンゲージメント時間の送信）。ただし**①を直さないと、そもそも計測の起点が遅れている**ので
順番を逆にしない。**まず①、次に②の確認、それから③。**

**→ ①と同日に実施。** Meta出稿前・次回開催の告知前という静かな時期に境界日を寄せるため、
先送りせずこの日にまとめた（出稿開始後に足すと、キャンペーンの途中に境界線が入る）。

- イベント名は `nav_click` 1つ。飛び先を `section` パラメータで持たせる
  （イベント名を増やすと境界日もGA4側の設定も増えて維持できなくなる）
- 対象は `.site-nav a` の7項目。**ヒーローの3ボタン等は2026-08-24に `button_click` として別途追加**
- **`section` はGA4のカスタムディメンションに登録しないとレポートに出ない。**
  登録前でもDebugViewとリアルタイムでは確認できる
- 狙い: 「主催者紹介を参加者の声の直後へ移す」改修の仮説が当たっているか。
  `section=champion` が押されているかで検証する
- **母数が小さいので短期で判断しない。** 出稿期間のデータを最初からきれいに残すための仕込み


### ④ 遅延ロードはエンゲージメント時間を削っていた（2026-08-20 整理）

**エンゲージメント時間の計測は、gtagが初期化された瞬間から始まる。** ページを開いてから
初期化までの時間（`window.load` 待ち＋最大8秒）は、**最初から存在しない**扱いになる。
さらにその時間は**イベントに便乗して送られる**ため、初期化前に離脱されると何も残らない。

広告からの流入は「着地してすぐ判断して帰る」人が多く、**この設計といちばん相性が悪い。**

※ `apply_click` が出ていなかったのは**別原因**（2026-08-06に `.js-apply-button` を
外したため）。上の「⚠️ `apply_click` は2026-08-06から発火していない」を参照。

**修正後、エンゲージメント時間とセッション数は増える。**
これは改善ではなく、計測の穴が塞がっただけ（上の「境界日」を参照）。

> **※ 修正前のLP側の実データは確認していない。**
> 「修正前は0だった」と読める記録を一度書いたが、根拠にしていたのは**Doorkeeper側**の
> データで、LP側の裏付けではなかったため取り下げた（⑤参照）。
> 上に書いてあるのは**仕組みから言えること**であって、実測ではない。

### ⑤ Doorkeeper側のデータの読み方（2026-08-20 確認）

**④とは別の話。** ④はLP側（`G-DSETWZS3SZ`／遅延ロードが原因・修正済み）、
こちらは**Doorkeeper側のプロパティ**で、**原因も対処も違う。**

Doorkeeper側は**エンゲージメント時間が全行0**だった（2026-05-22〜08-19の実データ）。
ただしこれは**Doorkeeperのタグ実装が `engagement_time_msec` を送っていない**ためで、
**こちらからは直せない。** LPの遅延ロードとは無関係。

結果、GA4の「エンゲージのあったセッション」が**「2ページ以上見たか」だけで判定されている**
（10秒以上の条件が成立しないため）。実際、キーイベントも全行0件だった。

- **「エンゲージメント率」を滞在の長さとして読まない。** 実質は「2ページ目を見た率」
- 流入元ごとに着地ページが違う（広告はイベントページ直）ため、率の横比較にも交絡がある
- だからこそ、**滞在ではなく「申込完了」を測る**（上のキーイベント化）ほうが早い

### ⑥ この規模では名簿のほうが精度が高い（2026-08-24）

**GA4で「初参加か再参加か」は区別できない。** Cookie単位のため、機種変更でも
同じ家庭の別端末でも別人になる。**Doorkeeperの参加者名簿には名前が載っており、
1回あたり4組程度なら手で数えられる。**

参加が同じ数家族の繰り返しなら、追うべきは申込数ではなく**初参加の組数**。

| 追う指標 | 読めること |
|---|---|
| 申込数 | 常連の再登録が混ざるため、増減の意味が読めない |
| **初参加の組数**（名簿から） | 新しい家庭に届いたかが直接分かる。広告の続行判断に使える |

#### `signup_complete` は「参加」ではない

同一トークンに `/completed` と `/cancel` の両方が存在する実例がある
（`/events/196200/tickets/-yS6yCU2-5BvbxvSzeMe/`）。
**申込後のキャンセルも1件として数える。**

#### referral経由の完了を「獲得」と読まないこと

2026-08-24時点で referral 経由の完了は5件（LP4・note1）あったが、
**常連の再登録である可能性が高い**（note経由の1件は第4回参加者と推定）。
`lp / referral` や `note.com / referral` は「もともと来る人がどの入口を通ったか」を
示しているだけで、新規獲得の証拠にはならない。


---

## 判断が要るもの（やらなくてもよい）

`apply_click` のパラメータが Universal Analytics 時代の書き方になっている。

```js
gtag("event", "apply_click", {
  event_category: "engagement",
  event_label: "doorkeeper_event",
  value: 1,
});
```

GA4では `event_category` / `event_label` は標準の意味を持たず、レポートに何も出ない。
害は無いので**そのままでもよい**。整理するなら消すだけでよく、**イベント名 `apply_click` は
変えないこと**（過去データとの連続性が切れる）。

---

## 触ってはいけないもの

| | 理由 |
|---|---|
| 測定ID `G-DSETWZS3SZ` | GA4プロパティ。変えると計測が別物になる |
| イベント名 `apply_click` | 過去データとの連続性。AGENTS.md にも記載あり |
| `.js-apply-button` のクラス名 | 計測の引っかかりどころ |
| Doorkeeperのutm（`utm_source=lp&utm_medium=referral&utm_campaign=coderdojo`） | AGENTS.md の同期ルール |

---

## 検証の作法（ブロマス側の教訓）

- **モバイル375pxで確認する。** 流入の大半がモバイル
- **`.click()` を直接呼ぶテストは `touchstart` を出さない。** モバイル固有の不具合を素通りする。
  touchstart / touchend を含むイベント列を出すこと
- **Braveでは検証できない。** シールドが `googletagmanager.com` をブロックするため、
  何も送信されない。**Chromeのシークレットウィンドウ（拡張機能オフ）で確認する**
- 確認方法: `gtag` をラップして引数を記録するか、ネットワークで `/g/collect` を見る。
  **ページを開いた直後に `page_view` が出るか**（8秒待たされないか）が今回の合否
